# Wallboard display update

* Prayer adhan and iqamah values are now displayed in 12 hour format with AM or PM.
* Fajr remains highlighted for 120 minutes after its adhan time, then clears until Dhuhr.
* The Fajr highlight duration is configurable using `fajrHighlightMinutes` in `resources/JS/config.js`.
