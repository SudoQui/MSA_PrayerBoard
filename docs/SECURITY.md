# Remote administration security

* Use named administrator accounts and least privilege.
* Prefer Tailscale SSH or an approved university remote access service.
* Do not expose SSH port 22 to the public internet.
* Do not enable remote root login.
* Keep the local administrator password strong and stored securely.
* Remove access immediately when an administrator leaves the project.
* Review the remote device list periodically and revoke unknown devices.
